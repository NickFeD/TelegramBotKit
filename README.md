# TelegramBotKit

A lightweight toolkit for building Telegram bots in .NET with:
- Commands (/start, text triggers)
- CallbackQuery routing
- Middleware pipeline
- WaitForUserResponse (simple next-message wait)
- Simple keyboard helpers

## Installation
```bash
dotnet add package TelegramBotKit
dotnet add package TelegramBotKit.Hosting
